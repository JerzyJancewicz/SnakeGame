/*
import java.util.Random;

public class RandomNumber {

    int RandomX = (int) (Math.random() * 1880);
    int RandomY = (int) (Math.random() * 1000);

    public int getRandomX() {
        if (RandomX % 40 == 0) {
            return RandomX;
        } else {
            while (RandomX % 40 != 0) {
                RandomX = (int) (Math.random() * 1880);
            }
            return RandomX;
        }
    }

    public int getRandomY() {
        if (RandomY % 40 == 0) {
            return RandomY;
        } else {
            while (RandomY % 40 != 0) {
                RandomY = (int) (Math.random() * 1000);
            }
            return RandomY;
        }
    }
}

 */
/*   Random randomX = new Random();
    Random randomY = new Random();


    public int getRandomX(){
        if (randomX.nextInt(1880) % 40 != 0) {
            while (randomX.nextInt(1880) % 40 != 0) {
                randomX.nextInt(1880);
            }
        }
        return randomX.nextInt(1880);
    }

    public int getRandomY(){
        if (randomY.nextInt(880) % 40 != 0) {
            while (randomY.nextInt(880) % 40 != 0) {
                randomY.nextInt(880);
            }
        }
        return randomY.nextInt(880);
    }
}
*/
/*
*/
